// The variables file for <%= name.capitalize %>

variables "tags" {
  type = "map"

  default = {
    team        = "unknown"
    product     = "unknown"
    service     = "unknown"
    environment = "uknown"
    owner       = "uknown"
  }
}
